<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical">

    <fragment
        android:id="@+id/mapfragment"
        class="com.here.android.mpa.mapping.AndroidXMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Map Fragment embedded with the map object -->

    <Button
        android:id="@+id/btn_createPTRouteButton"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Create Public Transit Route" />

    <Button
        android:id="@+id/btn_requestPTRouteButton"
        style="?android:attr/buttonStyleSmall"
        android:layout_below="@id/btn_createPTRouteButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Request Public Transit Route" />
</RelativeLayout>
